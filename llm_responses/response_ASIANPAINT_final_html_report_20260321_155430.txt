Stock: ASIANPAINT
Section: final_html_report
Timestamp: 2026-03-21 15:54:30.908806
Response Length: 9836 chars
================================================================================

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ASIANPAINT — Equity Analysis Report</title>
<style>
:root {
  --navy: #0D1B2A;
  --ink: #1A2B3C;
  --gold: #D4A017;
  --text: #2C3E50;
  --muted: #6B7C93;
  --light: #F5F7FA;
  --white: #FFFFFF;
  --green: #27AE60;
  --red: #C0392B;
  --border: #E0E6ED;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: 'Segoe UI', Arial, sans-serif;
  background: var(--light);
  color: var(--text);
  font-size: 13.5px;
  line-height: 1.75;
}

.page { max-width: 1060px; margin: 0 auto; background: var(--white); box-shadow: 0 4px 30px rgba(0,0,0,0.12); }

/* ===== HEADER ===== */
.header {
  background: linear-gradient(135deg, var(--navy) 0%, #1e3a5f 100%);
  color: var(--white);
  padding: 48px 52px 38px;
  position: relative;
  overflow: hidden;
}
.header::before {
  content: '';
  position: absolute;
  top: -60px; right: -60px;
  width: 300px; height: 300px;
  border-radius: 50%;
  background: rgba(212,160,23,0.07);
}
.header-top { display: flex; justify-content: space-between; align-items: flex-start; flex-wrap: wrap; gap: 20px; }
.header h1 { font-size: 42px; font-weight: 800; letter-spacing: 1px; color: var(--white); }
.header-subtitle { font-size: 15px; font-weight: 300; color: rgba(255,255,255,0.72); margin-top: 6px; }
.header-badge {
  background: rgba(212,160,23,0.18);
  border: 1px solid var(--gold);
  color: var(--gold);
  padding: 6px 16px;
  border-radius: 20px;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  white-space: nowrap;
}
.header-meta {
  display: flex;
  gap: 32px;
  margin-top: 28px;
  padding-top: 22px;
  border-top: 1px solid rgba(255,255,255,0.18);
  flex-wrap: wrap;
}
.meta-item { font-size: 12px; }
.meta-label { color: var(--gold); font-weight: 700; text-transform: uppercase; letter-spacing: 1px; display: block; margin-bottom: 3px; }
.meta-value { color: rgba(255,255,255,0.9); font-weight: 500; }

/* ===== RECOMMENDATION BOX ===== */
.recommendation-box {
  background: linear-gradient(135deg, #1a3a5c 0%, #0D1B2A 100%);
  border: 2px solid var(--gold);
  color: var(--white);
  padding: 30px 40px;
  margin: 32px 52px;
  border-radius: 10px;
  box-shadow: 0 6px 24px rgba(13,27,42,0.18);
}
.rec-header { display: flex; align-items: center; gap: 20px; margin-bottom: 22px; flex-wrap: wrap; }
.rec-label { font-size: 11px; text-transform: uppercase; letter-spacing: 2px; color: var(--gold); font-weight: 700; }
.rec-value { font-size: 32px; font-weight: 800; color: var(--gold); }
.rec-subtitle { font-size: 12px; color: rgba(255,255,255,0.65); margin-top: 2px; }
.recommendation-details {
  display: flex;
  gap: 0;
  border-top: 1px solid rgba(212,160,23,0.3);
  padding-top: 20px;
  flex-wrap: wrap;
}
.rec-detail {
  flex: 1;
  min-width: 120px;
  text-align: center;
  padding: 0 16px;
  border-right: 1px solid rgba(255,255,255,0.1);
}
.rec-detail:last-child { border-right: none; }
.rec-detail-label { font-size: 10px; opacity: 0.7; text-transform: uppercase; letter-spacing: 1px; }
.rec-detail-value { font-size: 20px; font-weight: 700; margin-top: 5px; color: var(--gold); }
.rec-detail-sub { font-size: 10px; color: rgba(255,255,255,0.55); margin-top: 2px; }

/* ===== CONTENT ===== */
.content { padding: 48px 52px; }

.section { margin-bottom: 56px; }

.section-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 28px;
  padding-bottom: 14px;
  border-bottom: 3px solid var(--gold);
}
.section-number {
  background: var(--navy);
  color: var(--gold);
  width: 36px; height: 36px;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 14px; font-weight: 700;
  flex-shrink: 0;
}
.section h2 { font-size: 22px; color: var(--navy); font-weight: 700; }

.subsection { margin-bottom: 38px; }
.subsection h3 {
  font-size: 16px;
  color: var(--ink);
  font-weight: 700;
  margin-bottom: 14px;
  padding-left: 12px;
  border-left: 4px solid var(--gold);
}
.subsection h4 {
  font-size: 13.5px;
  color: var(--text);
  font-weight: 700;
  margin: 18px 0 10px;
}

p { margin-bottom: 12px; line-height: 1.75; }

/* ===== TABLES ===== */
.table-wrap { overflow-x: auto; margin: 18px 0; border-radius: 6px; box-shadow: 0 2px 8px rgba(0,0,0,0.07); }
table {
  width: 100%;
  border-collapse: collapse;
  background: var(--white);
  font-size: 12.5px;
  min-width: 500px;
}
table caption {
  font-size: 13px;
  font-weight: 700;
  color: var(--navy);
  text-align: left;
  padding: 10px 14px 6px;
  background: #f0f4f8;
  border-bottom: 1px solid var(--border);
}
table thead tr { background: var(--navy); }
table th {
  padding: 11px 13px;
  text-align: left;
  font-weight: 600;
  color: var(--white);
  font-size: 11.5px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  white-space: nowrap;
}
table tbody tr { border-bottom: 1px solid var(--border); transition: background 0.15s; }
table tbody tr:nth-child(even) { background: #f8fafc; }
table tbody tr:hover { background: #eef3f8; }
table tbody td { padding: 10px 13px; vertical-align: middle; }
table tfoot td { padding: 10px 13px; font-weight: 700; background: #eef3f8; border-top: 2px solid var(--navy); }

.positive { color: var(--green); font-weight: 600; }
.negative { color: var(--red); font-weight: 600; }
.neutral { color: var(--muted); }
.bold { font-weight: 700; }
.highlight-row { background: #fff8e6 !important; font-weight: 700; }

/* ===== KEY TAKEAWAYS ===== */
.key-takeaways {
  background: linear-gradient(to right, #fff8e6, #fffdf5);
  border-left: 4px solid var(--gold);
  padding: 16px 20px;
  margin: 18px 0;
  border-radius: 0 6px 6px 0;
}
.key-takeaways .kt-title {
  font-size: 12px;
  font-weight: 700;
  color: var(--navy);
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 10px;
}
.key-takeaways ul { margin-left: 18px; }
.key-takeaways li { margin-bottom: 7px; line-height: 1.6; font-size: 13px; }

/* ===== CALLOUT BOXES ===== */
.callout {
  border-radius: 8px;
  padding: 16px 20px;
  margin: 16px 0;
  font-size: 13px;
}
.callout-blue { background: #e8f4fd; border-left: 4px solid #2980b9; }
.callout-green { background: #eafaf1; border-left: 4px solid var(--green); }
.callout-red { background: #fdf2f2; border-left: 4px solid var(--red); }
.callout-gold { background: #fff8e6; border-left: 4px solid var(--gold); }

/* ===== RISK MATRIX ===== */
.risk-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; margin: 16px 0; }
.risk-item {
  background: var(--light);
  border: 1px solid var(--border);
  border-radius: 6px;
  padding: 14px 16px;
}
.risk-item .risk-title { font-weight: 700; font-size: 13px; color: var(--navy); margin-bottom: 6px; }
.risk-item .risk-level { display: inline-block; padding: 2px 10px; border-radius: 12px; font-size: 11px; font-weight: 700; margin-bottom: 6px; }
.risk-high { background: #fde8e8; color: var(--red); }
.risk-medium { background: #fff3cd; color: #856404; }
.risk-low { background: #d4edda; color: #155724; }

/* ===== SHAREHOLDING VISUAL ===== */
.sh-bar { display: flex; height: 20px; border-radius: 4px; overflow: hidden; margin: 14px 0; }
.sh-segment { display: flex; align-items: center; justify-content: center; font-size: 10px; font-weight: 700; color: white; transition: all 0.3s; }

/* ===== VALUATION SUMMARY ===== */
.val-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 14px; margin: 20px 0; }
.val-card {
  background: var(--light);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 16px;
  text-align: center;
}
.val-card .method { font-size: 11px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.8px; margin-bottom: 8px; }
.val-card .value { font-size: 22px; font-weight: 800; color: var(--navy); }
.val-card .weight { font-size: 11px; color: var(--muted); margin-top: 4px; }
.val-card .updown { font-size: 12px; font-weight: 700; margin-top: 6px; }

.final-rec-box {
  background: linear-gradient(135deg, var(--navy) 0%, #1e3a5f 100%);
  color: var(--white);
  padding: 28px 32px;
  border-radius: 10px;
  margin: 24px 0;
  border: 2px solid var(--gold);
}
.final-rec-box h3 { color: var(--gold); font-size: 18px; margin-bottom: 16px; }
.final-rec-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-top: 18px; }
.final-rec-item { text-align: center; padding: 14px; background: rgba(255,255,255,0.06); border-radius: 6px; }
.final-rec-item .frl { font-size: 10px; color: rgba(255,255,255,0.6); text-transform: uppercase; letter-spacing: 1px; }
.final-rec-item .frv { font-size: 18px; font-weight: 800; color: var(--gold); margin-top: 4px; }

/* ===== METRIC BADGES ===== */
.trend-up { color: var(--green); font-weight: 700; }
.trend-down { color: var(--red); font-weight: 700; }
.trend-stable { color: #7f8c8d; font-weight: 600; }

/* ===== DIVIDER ===== */
.divider { border: none; border-top: 1px solid var(--border); margin: 28px 0; }

/* ===== FOOTER ===== */
.footer {
  background: var(--navy);
  color: rgba(255,255,255,0.75);
  padding: 28px 52px;
  font-size: 11px;
  line-height: 1.7;
  margin-top: 0;
}
.footer strong { color: var(--gold); }
.footer-grid { display: grid; grid-template-columns: 2fr 1fr; gap: 24px; }

/* ===== PRINT ===== */
@media print {
  body { background: white; font-size: 11px; }
  .page { box-shadow: none; max-width: 100%; }
  .section { page-break-inside: avoid; }
  .header { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
}

@media (max-width: 700px) {
  .header { padding: 28px 20px; }
  .content { padding: 24px 16px; }
  .recommendation-box { margin: 16px; padding: 20px; }
  .val-grid { grid-template-columns: 1fr 1fr; }
  .final-rec-grid { grid-template-columns: 1fr