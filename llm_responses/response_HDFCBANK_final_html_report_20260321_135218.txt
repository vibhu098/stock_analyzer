Stock: HDFCBANK
Section: final_html_report
Timestamp: 2026-03-21 13:52:18.888245
Response Length: 10754 chars
================================================================================

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>HDFC Bank (HDFCBANK) — Equity Research Report | 2026-03-21</title>
<style>
  /* ============================================================
     RESET & BASE
  ============================================================ */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --navy:       #1a3c6e;
    --navy-dark:  #0f2548;
    --navy-mid:   #0d4f8b;
    --blue-light: #2e6db4;
    --accent:     #f4a024;
    --accent-alt: #e8871a;
    --green:      #1a7a4a;
    --green-lt:   #d4edda;
    --red:        #b52a2a;
    --red-lt:     #fde8e8;
    --yellow-lt:  #fff8e6;
    --gray-bg:    #f5f7fa;
    --gray-mid:   #e2e8f0;
    --gray-dark:  #64748b;
    --text:       #1e293b;
    --text-mid:   #334155;
    --white:      #ffffff;
    --shadow:     0 2px 12px rgba(26,60,110,0.10);
    --shadow-lg:  0 6px 28px rgba(26,60,110,0.16);
    --radius:     8px;
    --radius-lg:  14px;
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
    font-size: 14px;
    line-height: 1.7;
    color: var(--text);
    background: var(--gray-bg);
  }

  /* ============================================================
     LAYOUT WRAPPER
  ============================================================ */
  .page-wrapper {
    max-width: 1180px;
    margin: 0 auto;
    padding: 0 18px 60px;
  }

  /* ============================================================
     REPORT HEADER
  ============================================================ */
  .report-header {
    background: linear-gradient(135deg, var(--navy-dark) 0%, var(--navy) 45%, var(--navy-mid) 100%);
    color: var(--white);
    padding: 0;
    margin-bottom: 32px;
    border-radius: 0 0 var(--radius-lg) var(--radius-lg);
    box-shadow: var(--shadow-lg);
    overflow: hidden;
  }

  .header-top-bar {
    background: rgba(0,0,0,0.22);
    padding: 10px 36px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 11.5px;
    letter-spacing: 0.04em;
    color: rgba(255,255,255,0.78);
  }

  .header-top-bar span { display: flex; align-items: center; gap: 6px; }

  .header-main {
    padding: 36px 36px 28px;
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 24px;
    flex-wrap: wrap;
  }

  .header-identity { flex: 1; min-width: 280px; }

  .header-sector-tag {
    display: inline-block;
    background: rgba(244,160,36,0.22);
    border: 1px solid rgba(244,160,36,0.5);
    color: var(--accent);
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 3px 10px;
    border-radius: 20px;
    margin-bottom: 10px;
  }

  .header-company-name {
    font-size: 34px;
    font-weight: 800;
    letter-spacing: -0.5px;
    line-height: 1.1;
    margin-bottom: 4px;
  }

  .header-ticker {
    font-size: 15px;
    color: rgba(255,255,255,0.65);
    font-weight: 500;
    margin-bottom: 14px;
  }

  .header-ticker strong {
    color: var(--accent);
    font-weight: 700;
    font-size: 16px;
  }

  .header-desc {
    font-size: 13px;
    color: rgba(255,255,255,0.72);
    max-width: 520px;
    line-height: 1.6;
  }

  .header-right { display: flex; flex-direction: column; align-items: flex-end; gap: 16px; min-width: 220px; }

  .recommendation-badge {
    background: linear-gradient(135deg, var(--accent) 0%, var(--accent-alt) 100%);
    color: var(--white);
    font-size: 22px;
    font-weight: 900;
    letter-spacing: 0.06em;
    padding: 10px 28px;
    border-radius: var(--radius);
    text-transform: uppercase;
    box-shadow: 0 4px 16px rgba(244,160,36,0.40);
    text-align: center;
  }

  .recommendation-badge small {
    display: block;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.12em;
    opacity: 0.88;
    margin-bottom: 2px;
  }

  .target-price-box {
    text-align: right;
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: var(--radius);
    padding: 12px 18px;
  }

  .target-price-box .label { font-size: 10.5px; color: rgba(255,255,255,0.6); letter-spacing: 0.08em; text-transform: uppercase; }
  .target-price-box .value { font-size: 26px; font-weight: 800; color: var(--accent); line-height: 1.1; }
  .target-price-box .sub { font-size: 11.5px; color: rgba(255,255,255,0.55); margin-top: 2px; }

  .header-metrics-bar {
    background: rgba(0,0,0,0.18);
    border-top: 1px solid rgba(255,255,255,0.10);
    padding: 18px 36px;
    display: flex;
    gap: 0;
    flex-wrap: wrap;
  }

  .hm-item {
    flex: 1;
    min-width: 120px;
    padding: 0 20px;
    border-right: 1px solid rgba(255,255,255,0.12);
    text-align: center;
  }

  .hm-item:first-child { padding-left: 0; }
  .hm-item:last-child { border-right: none; }

  .hm-item .hm-label {
    font-size: 10px;
    color: rgba(255,255,255,0.55);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 3px;
  }

  .hm-item .hm-value {
    font-size: 17px;
    font-weight: 800;
    color: var(--white);
    line-height: 1.1;
  }

  .hm-item .hm-sub {
    font-size: 10px;
    color: rgba(255,255,255,0.5);
    margin-top: 2px;
  }

  .hm-positive { color: #5de8a0 !important; }
  .hm-negative { color: #ff8080 !important; }
  .hm-accent   { color: var(--accent) !important; }

  /* ============================================================
     TABLE OF CONTENTS
  ============================================================ */
  .toc-card {
    background: var(--white);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow);
    padding: 24px 30px;
    margin-bottom: 28px;
    border-left: 5px solid var(--navy);
  }

  .toc-card h3 {
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--navy);
    margin-bottom: 14px;
    font-weight: 700;
  }

  .toc-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 8px 20px;
  }

  .toc-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 7px 0;
    border-bottom: 1px solid var(--gray-mid);
    text-decoration: none;
    color: var(--text-mid);
    font-size: 13px;
    font-weight: 500;
    transition: color 0.2s;
  }

  .toc-item:hover { color: var(--navy-mid); }

  .toc-num {
    width: 24px;
    height: 24px;
    background: var(--navy);
    color: var(--white);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 11px;
    font-weight: 700;
    flex-shrink: 0;
  }

  /* ============================================================
     SECTION CARDS
  ============================================================ */
  .section-card {
    background: var(--white);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow);
    margin-bottom: 32px;
    overflow: hidden;
  }

  .section-header {
    background: linear-gradient(135deg, var(--navy) 0%, var(--navy-mid) 100%);
    color: var(--white);
    padding: 18px 30px;
    display: flex;
    align-items: center;
    gap: 14px;
  }

  .section-num {
    width: 38px;
    height: 38px;
    background: rgba(255,255,255,0.15);
    border: 2px solid rgba(255,255,255,0.3);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 15px;
    font-weight: 800;
    flex-shrink: 0;
  }

  .section-title-block {}
  .section-title-block h2 {
    font-size: 18px;
    font-weight: 800;
    letter-spacing: -0.2px;
    line-height: 1.2;
  }
  .section-title-block .section-subtitle {
    font-size: 11.5px;
    color: rgba(255,255,255,0.65);
    margin-top: 2px;
  }

  .section-body {
    padding: 28px 30px;
  }

  /* ============================================================
     TYPOGRAPHY INSIDE SECTIONS
  ============================================================ */
  .section-body h3 {
    font-size: 16px;
    font-weight: 700;
    color: var(--navy);
    margin: 24px 0 10px;
    padding-bottom: 6px;
    border-bottom: 2px solid var(--gray-mid);
  }

  .section-body h3:first-child { margin-top: 0; }

  .section-body h4 {
    font-size: 14px;
    font-weight: 700;
    color: var(--navy-mid);
    margin: 18px 0 8px;
  }

  .section-body p {
    color: var(--text-mid);
    margin-bottom: 12px;
    font-size: 13.5px;
    line-height: 1.75;
  }

  .section-body ul, .section-body ol {
    margin: 8px 0 14px 20px;
    color: var(--text-mid);
  }

  .section-body li {
    margin-bottom: 7px;
    font-size: 13.5px;
    line-height: 1.65;
  }

  .section-body strong { color: var(--text); }

  .section-body em {
    color: var(--gray-dark);
    font-size: 12.5px;
  }

  hr {
    border: none;
    border-top: 1px solid var(--gray-mid);
    margin: 24px 0;
  }

  /* ============================================================
     TABLES — BASE
  ============================================================ */
  .section-body table,
  table.metrics-table,
  table.shareholding-table,
  table.stats-table,
  table.driver-table,
  table.catalysts-table,
  table.risk-analysis-table,
  table.metrics-summary-table {
    width: 100%;
    border-collapse: collapse;
    margin: 14px 0 20px;
    font-size: 13px;
    border-radius: var(--radius);
    overflow: hidden;
    box-shadow: 0 1px 6px rgba(26,60,110,0.07);
  }

  .section-body table thead tr,
  table.metrics-table thead tr,
  table.shareholding-table thead tr,
  table.stats-table thead tr,
  table.driver-table thead tr,
  table.catalysts-table thead tr,
  table.risk-analysis-table thead tr,
  table.metrics-summary-table thead tr {
    background: linear-gradient(135deg, var(--navy) 0%, var(--navy-mid) 100%);
    color: var(--white);
  }

  .section-body table th,
  table.metrics-table th,
  table.shareholding-table th,
  table.stats-table th,
  table.driver-table th,
  table.catalysts-table th,
  table.risk-analysis-table th,
  table.metrics-summary-table th {
    padding: 11px 14px;
    text-align: left;
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 0.03em;
    white-space: nowrap;
  }

  .section-body table td,
  table.metrics-table td,
  table.shareholding-table td,
  table.stats-table td,
  table.driver-table td,
  table.catalysts-table td,
  table.risk-analysis-table td,
  table.metrics-summary-table td {
    padding: 10px 14px;
    border-bottom: 1px solid var(--gray-mid);
    vertical-align: top;
    line-height: 1.55;
    color: var(--text-mid);
  }

  .section-body table tbody tr:nth-child(even),
  table.metrics-table tbody tr:nth-child(