Stock: ASIANPAINT
Section: final_html_report
Timestamp: 2026-03-21 15:39:04.527898
Response Length: 9938 chars
================================================================================

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ASIANPAINT — Equity Analysis Report</title>
<style>
:root {
  --navy: #0D1B2A;
  --ink: #1A2B3C;
  --gold: #D4A017;
  --text: #2C3E50;
  --muted: #6B7C93;
  --light: #F5F7FA;
  --white: #FFFFFF;
  --green: #27AE60;
  --red: #C0392B;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: 'Segoe UI', Arial, sans-serif;
  background: var(--light);
  color: var(--text);
  font-size: 14px;
  line-height: 1.7;
}

.page { max-width: 1100px; margin: 0 auto; background: var(--white); box-shadow: 0 4px 30px rgba(0,0,0,0.1); }

.header {
  background: linear-gradient(135deg, var(--navy) 0%, var(--ink) 100%);
  color: var(--white);
  padding: 50px 50px 40px;
  position: relative;
  overflow: hidden;
}

.header::before {
  content: '';
  position: absolute;
  top: -50px; right: -50px;
  width: 300px; height: 300px;
  border-radius: 50%;
  background: rgba(212,160,23,0.08);
}

.header h1 {
  font-size: 44px;
  font-weight: bold;
  margin-bottom: 6px;
  letter-spacing: 2px;
}

.header .subtitle {
  font-size: 16px;
  font-weight: 300;
  color: rgba(255,255,255,0.7);
  margin-top: 4px;
}

.header-meta {
  display: flex;
  gap: 35px;
  margin-top: 28px;
  padding-top: 22px;
  border-top: 1px solid rgba(255,255,255,0.2);
  flex-wrap: wrap;
}

.meta-item { font-size: 12px; }
.meta-label { color: var(--gold); font-weight: bold; text-transform: uppercase; letter-spacing: 1px; display: block; margin-bottom: 3px; }

.recommendation-box {
  background: linear-gradient(135deg, #1a3a5c 0%, #0D1B2A 100%);
  border: 2px solid var(--gold);
  color: var(--white);
  padding: 30px 40px;
  margin: 30px 50px;
  border-radius: 10px;
  text-align: center;
  box-shadow: 0 6px 20px rgba(13,27,42,0.3);
}

.recommendation-box .rec-label {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 3px;
  margin-bottom: 10px;
  color: var(--gold);
}

.recommendation-box .rec-value {
  font-size: 32px;
  font-weight: bold;
  margin-bottom: 5px;
  color: #FFD700;
}

.rec-subtitle { font-size: 13px; color: rgba(255,255,255,0.7); margin-bottom: 20px; }

.recommendation-details {
  display: flex;
  gap: 0;
  justify-content: center;
  margin-top: 20px;
  flex-wrap: wrap;
  border-top: 1px solid rgba(255,255,255,0.15);
  padding-top: 20px;
}

.rec-detail {
  text-align: center;
  padding: 0 30px;
  border-right: 1px solid rgba(255,255,255,0.15);
}

.rec-detail:last-child { border-right: none; }
.rec-detail-label { font-size: 10px; color: rgba(255,255,255,0.6); text-transform: uppercase; letter-spacing: 1px; }
.rec-detail-value { font-size: 20px; font-weight: bold; margin-top: 4px; color: var(--gold); }

.content { padding: 50px; }

.section {
  margin-bottom: 55px;
}

.section-header {
  background: linear-gradient(90deg, var(--navy) 0%, var(--ink) 100%);
  color: var(--white);
  padding: 14px 22px;
  border-radius: 6px;
  margin-bottom: 28px;
  display: flex;
  align-items: center;
  gap: 15px;
}

.section-number {
  background: var(--gold);
  color: var(--navy);
  width: 32px; height: 32px;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-weight: bold; font-size: 14px;
  flex-shrink: 0;
}

.section-title { font-size: 20px; font-weight: 600; }

h3 {
  font-size: 17px;
  color: var(--navy);
  margin-top: 28px;
  margin-bottom: 14px;
  padding-bottom: 6px;
  border-bottom: 2px solid var(--gold);
}

h4 {
  font-size: 14px;
  color: var(--ink);
  font-weight: bold;
  margin-top: 18px;
  margin-bottom: 10px;
}

p { margin-bottom: 12px; }

table {
  width: 100%;
  border-collapse: collapse;
  margin: 18px 0;
  background: var(--white);
  font-size: 12.5px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.06);
  border-radius: 6px;
  overflow: hidden;
}

table caption {
  font-weight: bold;
  font-size: 13px;
  color: var(--navy);
  padding: 10px 0;
  text-align: left;
}

table thead tr { background: var(--navy); color: var(--white); }
table th { padding: 11px 13px; text-align: left; font-weight: 600; font-size: 12px; }
table tbody tr { border-bottom: 1px solid #E8ECF0; }
table tbody tr:nth-child(even) { background: #F8FAFB; }
table tbody tr:hover { background: #EEF2F7; }
table tbody td { padding: 9px 13px; }

.positive { color: var(--green); font-weight: bold; }
.negative { color: var(--red); font-weight: bold; }
.neutral { color: var(--muted); }
.highlight { background: #FFF9E6 !important; }

.key-takeaways {
  background: linear-gradient(135deg, #F8FAFB 0%, #EEF2F7 100%);
  border-left: 4px solid var(--gold);
  padding: 18px 20px;
  margin: 18px 0;
  border-radius: 0 6px 6px 0;
}

.key-takeaways .kt-title {
  font-weight: bold;
  color: var(--navy);
  font-size: 13px;
  margin-bottom: 10px;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.key-takeaways ul { margin-left: 20px; }
.key-takeaways li { margin-bottom: 7px; line-height: 1.5; font-size: 13px; }

.alert-box {
  background: #FFF3CD;
  border: 1px solid #FFD700;
  border-left: 4px solid var(--gold);
  padding: 14px 18px;
  margin: 15px 0;
  border-radius: 4px;
  font-size: 13px;
}

.info-box {
  background: #E8F4FD;
  border-left: 4px solid #2980B9;
  padding: 14px 18px;
  margin: 15px 0;
  border-radius: 0 4px 4px 0;
  font-size: 13px;
}

.two-col {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  margin: 15px 0;
}

.metric-card {
  background: var(--light);
  border: 1px solid #DDE3EC;
  border-radius: 8px;
  padding: 16px;
  text-align: center;
}

.metric-card .mc-label { font-size: 11px; color: var(--muted); text-transform: uppercase; letter-spacing: 1px; }
.metric-card .mc-value { font-size: 24px; font-weight: bold; color: var(--navy); margin: 6px 0; }
.metric-card .mc-sub { font-size: 11px; color: var(--muted); }

.divider {
  border: none;
  border-top: 1px solid #E0E6EE;
  margin: 30px 0;
}

ul, ol { margin-left: 22px; margin-bottom: 12px; }
li { margin-bottom: 5px; line-height: 1.6; }

.subsection { margin-bottom: 35px; }

.valuation-summary {
  background: linear-gradient(135deg, var(--navy) 0%, #1a3a5c 100%);
  color: var(--white);
  padding: 25px 30px;
  border-radius: 8px;
  margin: 20px 0;
}

.valuation-summary h4 { color: var(--gold); margin-top: 0; }

.final-rec-box {
  background: linear-gradient(135deg, #0D1B2A 0%, #1A2B3C 100%);
  border: 2px solid var(--gold);
  color: var(--white);
  padding: 30px;
  border-radius: 10px;
  margin: 25px 0;
}

.final-rec-box .fr-title { color: var(--gold); font-size: 12px; text-transform: uppercase; letter-spacing: 2px; margin-bottom: 8px; }
.final-rec-box .fr-rec { font-size: 28px; font-weight: bold; color: #FFD700; margin-bottom: 5px; }
.final-rec-box .fr-target { font-size: 18px; color: rgba(255,255,255,0.9); margin-bottom: 15px; }

.risk-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 15px;
  margin: 15px 0;
}

.risk-card {
  padding: 15px;
  border-radius: 6px;
  font-size: 13px;
}

.risk-card.high { background: #FDE8E8; border-left: 4px solid var(--red); }
.risk-card.medium { background: #FFF3CD; border-left: 4px solid #F39C12; }
.risk-card.low { background: #E8F8EE; border-left: 4px solid var(--green); }

.risk-card .rc-title { font-weight: bold; margin-bottom: 6px; font-size: 13px; }

.footer {
  background: var(--navy);
  color: rgba(255,255,255,0.75);
  padding: 30px 50px;
  font-size: 11px;
  line-height: 1.7;
  margin-top: 50px;
}

.footer strong { color: var(--gold); }

.toc {
  background: var(--light);
  border: 1px solid #DDE3EC;
  border-radius: 8px;
  padding: 20px 25px;
  margin: 0 50px 30px;
}

.toc h3 { font-size: 14px; margin-top: 0; border: none; padding: 0; color: var(--navy); }
.toc ol { margin-left: 20px; }
.toc li { margin-bottom: 4px; font-size: 13px; color: var(--text); }

@media print {
  body { background: white; font-size: 11px; }
  .page { box-shadow: none; max-width: 100%; }
  .section { page-break-inside: avoid; }
  table { font-size: 10px; }
}
</style>
</head>
<body>

<div class="page">

  <!-- HEADER -->
  <div class="header">
    <h1>ASIANPAINT</h1>
    <div class="subtitle">Asian Paints Limited — Comprehensive Equity Research Report</div>
    <div class="header-meta">
      <div class="meta-item"><span class="meta-label">Report Date</span>March 21, 2026</div>
      <div class="meta-item"><span class="meta-label">Exchange</span>NSE / BSE</div>
      <div class="meta-item"><span class="meta-label">Sector</span>Consumer Discretionary — Paints &amp; Coatings</div>
      <div class="meta-item"><span class="meta-label">BSE Code</span>500820</div>
      <div class="meta-item"><span class="meta-label">Analysis Type</span>Fundamental Equity Research</div>
      <div class="meta-item"><span class="meta-label">Data Through</span>Sep 2025 (H1 FY26)</div>
      <div class="meta-item"><span class="meta-label">Coverage</span>Initiation of Coverage</div>
    </div>
  </div>

  <!-- RECOMMENDATION BOX -->
  <div class="recommendation-box">
    <div class="rec-label">Investment Recommendation</div>
    <div class="rec-value">ACCUMULATE</div>
    <div class="rec-subtitle">Quality Compounder | Build on Dips | 24–36 Month Horizon</div>
    <div class="recommendation-details">
      <div class="rec-detail">
        <div class="rec-detail-label">Current Price (CMP)</div>
        <div class="rec-detail-value">₹2,350</div>
      </div>
      <div class="rec-detail">
        <div class="rec-detail-label">12-Month Target</div>
        <div class="rec-detail-value">₹2,600</div>
      </div>
      <div class="rec-detail">
        <div class="rec-detail-label">Upside Potential</div>
        <div class="rec-detail-value">+10.6%</div>
      </div>
      <div class="rec-detail">
        <div class="rec-detail-label">3-Year Target</div>
        <div class="rec-detail-value">₹3,200</div>
      </div>
      <div class="rec-detail">
        