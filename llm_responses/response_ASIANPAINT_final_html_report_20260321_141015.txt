Stock: ASIANPAINT
Section: final_html_report
Timestamp: 2026-03-21 14:10:15.338443
Response Length: 9539 chars
================================================================================

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Asian Paints (ASIANPAINT) — Equity Research Report | 2026-03-21</title>
<style>
  /* ===== RESET & BASE ===== */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { font-size: 15px; scroll-behavior: smooth; }
  body {
    font-family: 'Segoe UI', Arial, sans-serif;
    background: #f0f4f9;
    color: #1a2236;
    line-height: 1.7;
  }

  /* ===== TYPOGRAPHY ===== */
  h1 { font-size: 2rem; font-weight: 800; letter-spacing: -0.5px; }
  h2 { font-size: 1.45rem; font-weight: 700; color: #1a3c6e; margin-bottom: 0.6rem; }
  h3 { font-size: 1.15rem; font-weight: 700; color: #0d4f8b; margin: 1.2rem 0 0.5rem; }
  h4 { font-size: 1rem; font-weight: 600; color: #1a3c6e; margin: 1rem 0 0.4rem; }
  p { margin-bottom: 0.85rem; color: #2c3e50; }
  ul, ol { padding-left: 1.4rem; margin-bottom: 0.85rem; }
  li { margin-bottom: 0.35rem; color: #2c3e50; }
  strong { color: #1a3c6e; }
  em { color: #555; }
  a { color: #0d4f8b; }

  /* ===== LAYOUT ===== */
  .page-wrapper { max-width: 1200px; margin: 0 auto; padding: 0 1rem 3rem; }

  /* ===== HEADER ===== */
  .report-header {
    background: linear-gradient(135deg, #0d2a55 0%, #1a3c6e 45%, #0d4f8b 100%);
    color: #fff;
    padding: 2.5rem 2.5rem 2rem;
    border-radius: 0 0 18px 18px;
    margin-bottom: 2rem;
    position: relative;
    overflow: hidden;
  }
  .report-header::before {
    content: '';
    position: absolute;
    top: -60px; right: -60px;
    width: 260px; height: 260px;
    background: rgba(244,160,36,0.12);
    border-radius: 50%;
  }
  .report-header::after {
    content: '';
    position: absolute;
    bottom: -80px; left: 60px;
    width: 200px; height: 200px;
    background: rgba(255,255,255,0.05);
    border-radius: 50%;
  }
  .header-top { display: flex; justify-content: space-between; align-items: flex-start; flex-wrap: wrap; gap: 1rem; }
  .header-company h1 { color: #fff; font-size: 2.1rem; }
  .header-company .ticker {
    display: inline-block;
    background: #f4a024;
    color: #fff;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 1.5px;
    padding: 0.2rem 0.7rem;
    border-radius: 4px;
    margin-top: 0.3rem;
  }
  .header-company .subtitle { color: rgba(255,255,255,0.75); font-size: 0.9rem; margin-top: 0.4rem; }
  .header-meta { text-align: right; }
  .report-date { color: rgba(255,255,255,0.7); font-size: 0.85rem; margin-bottom: 0.5rem; }
  .recommendation-badge {
    display: inline-block;
    background: linear-gradient(135deg, #f4a024, #e8890a);
    color: #fff;
    font-size: 1rem;
    font-weight: 800;
    letter-spacing: 1px;
    padding: 0.5rem 1.4rem;
    border-radius: 30px;
    box-shadow: 0 4px 15px rgba(244,160,36,0.4);
    text-transform: uppercase;
  }
  .header-metrics {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 1rem;
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(255,255,255,0.15);
  }
  .metric-card {
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 10px;
    padding: 0.8rem 1rem;
    backdrop-filter: blur(4px);
  }
  .metric-card .label { font-size: 0.7rem; color: rgba(255,255,255,0.6); letter-spacing: 0.8px; text-transform: uppercase; margin-bottom: 0.2rem; }
  .metric-card .value { font-size: 1.15rem; font-weight: 700; color: #fff; }
  .metric-card .sub { font-size: 0.72rem; color: rgba(255,255,255,0.55); margin-top: 0.1rem; }
  .metric-card.accent .value { color: #f4a024; }

  /* ===== TOC ===== */
  .toc-bar {
    background: #fff;
    border-radius: 12px;
    padding: 1rem 1.5rem;
    margin-bottom: 2rem;
    box-shadow: 0 2px 12px rgba(26,60,110,0.08);
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    align-items: center;
  }
  .toc-bar span { font-size: 0.78rem; font-weight: 700; color: #1a3c6e; margin-right: 0.5rem; text-transform: uppercase; letter-spacing: 0.8px; }
  .toc-bar a {
    font-size: 0.78rem;
    color: #0d4f8b;
    text-decoration: none;
    padding: 0.25rem 0.7rem;
    border-radius: 20px;
    border: 1px solid #c5d8f0;
    background: #f0f6ff;
    transition: all 0.2s;
  }
  .toc-bar a:hover { background: #1a3c6e; color: #fff; border-color: #1a3c6e; }

  /* ===== SECTION CARDS ===== */
  .section-card {
    background: #fff;
    border-radius: 14px;
    box-shadow: 0 2px 16px rgba(26,60,110,0.07);
    margin-bottom: 2rem;
    overflow: hidden;
    border-left: 5px solid #1a3c6e;
  }
  .section-header {
    background: linear-gradient(90deg, #1a3c6e 0%, #0d4f8b 100%);
    color: #fff;
    padding: 1rem 1.8rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }
  .section-number {
    background: #f4a024;
    color: #fff;
    font-size: 0.8rem;
    font-weight: 800;
    width: 32px; height: 32px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
    box-shadow: 0 2px 8px rgba(244,160,36,0.4);
  }
  .section-header h2 { color: #fff; font-size: 1.15rem; margin: 0; }
  .section-body { padding: 1.8rem; }

  /* ===== TABLES ===== */
  .table-wrapper { overflow-x: auto; margin: 1rem 0 1.2rem; border-radius: 10px; box-shadow: 0 1px 8px rgba(26,60,110,0.07); }
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.85rem;
  }
  table thead tr {
    background: linear-gradient(90deg, #1a3c6e, #0d4f8b);
    color: #fff;
  }
  table thead th {
    padding: 0.75rem 1rem;
    text-align: left;
    font-weight: 600;
    font-size: 0.8rem;
    letter-spacing: 0.4px;
    white-space: nowrap;
  }
  table tbody tr { transition: background 0.15s; }
  table tbody tr:nth-child(even) { background: #f5f8fd; }
  table tbody tr:nth-child(odd) { background: #fff; }
  table tbody tr:hover { background: #e8f0fb; }
  table tbody td {
    padding: 0.65rem 1rem;
    border-bottom: 1px solid #e8eef7;
    color: #2c3e50;
    vertical-align: middle;
  }
  table tbody tr:last-child td { border-bottom: none; }

  /* ===== SPECIAL TABLE VARIANTS ===== */
  .highlight-row td { background: #fff8e1 !important; font-weight: 600; }
  .positive { color: #1a7a4a !important; font-weight: 700; }
  .negative { color: #c0392b !important; font-weight: 700; }
  .neutral { color: #888 !important; }

  /* ===== RECOMMENDATION TABLE ===== */
  .rec-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    margin: 1rem 0;
  }
  .rec-item {
    border-radius: 10px;
    padding: 1rem 1.2rem;
    border: 1px solid #e0e8f5;
  }
  .rec-item.positive-bg { background: linear-gradient(135deg, #e8f5e9, #f1faf2); border-color: #a5d6a7; }
  .rec-item.negative-bg { background: linear-gradient(135deg, #fdecea, #fff5f5); border-color: #ef9a9a; }
  .rec-item.neutral-bg { background: linear-gradient(135deg, #fff8e1, #fffde7); border-color: #ffe082; }
  .rec-item .rec-label { font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.8px; font-weight: 700; color: #888; margin-bottom: 0.3rem; }
  .rec-item .rec-value { font-size: 1rem; font-weight: 700; color: #1a3c6e; }
  .rec-item .rec-sub { font-size: 0.75rem; color: #666; margin-top: 0.2rem; }

  /* ===== CALLOUT BOXES ===== */
  .callout {
    border-radius: 10px;
    padding: 1rem 1.3rem;
    margin: 1rem 0;
    font-size: 0.88rem;
    display: flex;
    gap: 0.8rem;
    align-items: flex-start;
  }
  .callout-icon { font-size: 1.2rem; flex-shrink: 0; margin-top: 0.1rem; }
  .callout.info { background: #e8f0fb; border-left: 4px solid #0d4f8b; }
  .callout.warning { background: #fff8e1; border-left: 4px solid #f4a024; }
  .callout.danger { background: #fdecea; border-left: 4px solid #e53935; }
  .callout.success { background: #e8f5e9; border-left: 4px solid #2e7d32; }
  .callout p { margin: 0; color: #1a2236; }

  /* ===== RATING STARS ===== */
  .stars { color: #f4a024; letter-spacing: 1px; }

  /* ===== VERDICT BOX ===== */
  .verdict-box {
    background: linear-gradient(135deg, #0d2a55 0%, #1a3c6e 100%);
    color: #fff;
    border-radius: 14px;
    padding: 2rem;
    margin: 1.5rem 0;
    position: relative;
    overflow: hidden;
  }
  .verdict-box::before {
    content: '';
    position: absolute;
    top: -40px; right: -40px;
    width: 180px; height: 180px;
    background: rgba(244,160,36,0.1);
    border-radius: 50%;
  }
  .verdict-box h3 { color: #f4a024; margin-top: 0; }
  .verdict-box p { color: rgba(255,255,255,0.88); margin-bottom: 0.7rem; }
  .verdict-box strong { color: #f4a024; }

  /* ===== PRICE TARGET BOX ===== */
  .price-target-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin: 1.2rem 0;
  }
  .pt-card {
    border-radius: 12px;
    padding: 1.2rem;
    text-align: center;
    border: 2px solid;
  }
  .pt-card.bear { background: #fff5f5; border-color: #e53935; }
  .pt-card.base { background: #f0f6ff; border-color: #0d4f8b; }
  .pt-card.bull { background: #f1faf2; border-color: #2e7d32; }
  .pt-card .pt-label { font-size: 0.72rem; text-transform: uppercase; letter-spacing: 1px; font-weight: 700; margin-bottom: 0.4rem; }
  .pt-card.bear .pt-label { color: #e53935; }
  .pt-card.base .pt-label { color: #0d4f8b; }
  .pt-card.bull .pt-label { color: #2e7d32; }
  .pt-card .pt-price { font-size: 1.6rem; font-weight: 800; color: #1a3c6e; }
  .pt-card .pt-sub { font-size: 0.75rem; color: #666; margin-top: 0.3rem; }

  /* =====