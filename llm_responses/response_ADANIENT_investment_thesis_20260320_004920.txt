Stock: ADANIENT
Section: investment_thesis
Timestamp: 2026-03-20 00:49:20.295442
Response Length: 9752 chars
================================================================================

# Adani Enterprises Limited (ADANIENT) — Investment Thesis

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ADANIENT - Investment Thesis</title>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: #f0f2f5;
    color: #1a1a2e;
    line-height: 1.65;
    font-size: 14px;
  }

  .report-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 28px 24px;
  }

  /* ── HEADER ── */
  .report-header {
    background: linear-gradient(135deg, #0d1b2a 0%, #1b3a5c 55%, #0d3b6e 100%);
    border-radius: 16px;
    padding: 40px 44px;
    margin-bottom: 32px;
    position: relative;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(13,27,42,0.35);
  }
  .report-header::before {
    content: '';
    position: absolute;
    top: -60px; right: -60px;
    width: 260px; height: 260px;
    background: rgba(244,160,36,0.10);
    border-radius: 50%;
  }
  .report-header::after {
    content: '';
    position: absolute;
    bottom: -40px; left: -40px;
    width: 180px; height: 180px;
    background: rgba(255,255,255,0.04);
    border-radius: 50%;
  }
  .header-badge {
    display: inline-block;
    background: rgba(244,160,36,0.18);
    border: 1px solid rgba(244,160,36,0.45);
    color: #f4a024;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    padding: 5px 14px;
    border-radius: 20px;
    margin-bottom: 14px;
  }
  .report-header h1 {
    font-size: 28px;
    font-weight: 800;
    color: #ffffff;
    letter-spacing: 0.4px;
    margin-bottom: 6px;
  }
  .report-header h1 span { color: #f4a024; }
  .report-header .subtitle {
    font-size: 14px;
    color: rgba(255,255,255,0.65);
    margin-bottom: 22px;
  }
  .header-meta {
    display: flex;
    gap: 28px;
    flex-wrap: wrap;
  }
  .meta-chip {
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 8px;
    padding: 8px 16px;
    color: #fff;
    font-size: 12px;
  }
  .meta-chip strong { color: #f4a024; display: block; font-size: 15px; }

  /* ── SECTION CARD ── */
  .section-card {
    background: #ffffff;
    border-radius: 14px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.07);
    margin-bottom: 28px;
    overflow: hidden;
  }
  .section-header {
    background: linear-gradient(90deg, #1a3c6e 0%, #1e4d8c 100%);
    padding: 16px 28px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .section-number {
    background: #f4a024;
    color: #fff;
    font-size: 13px;
    font-weight: 800;
    width: 30px; height: 30px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
  }
  .section-header h2 {
    font-size: 16px;
    font-weight: 700;
    color: #ffffff;
    letter-spacing: 0.3px;
  }
  .section-body { padding: 28px 32px; }

  /* ── EXECUTIVE HOOK ── */
  .exec-hook {
    background: linear-gradient(135deg, #fffbf0, #fff8e6);
    border-left: 5px solid #f4a024;
    border-radius: 0 10px 10px 0;
    padding: 20px 24px;
    margin-bottom: 26px;
    font-size: 15px;
    font-weight: 600;
    color: #1a1a2e;
    line-height: 1.7;
  }
  .exec-hook span { color: #1a3c6e; }

  /* ── TABLES ── */
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 13px;
    margin-top: 4px;
  }
  thead tr {
    background: linear-gradient(90deg, #1a3c6e, #1e4d8c);
    color: #fff;
  }
  thead th {
    padding: 11px 14px;
    text-align: left;
    font-weight: 700;
    font-size: 12px;
    letter-spacing: 0.4px;
    text-transform: uppercase;
  }
  tbody tr { border-bottom: 1px solid #eef0f5; }
  tbody tr:nth-child(even) { background: #f8f9fc; }
  tbody tr:hover { background: #eef3fb; transition: background 0.2s; }
  tbody td {
    padding: 11px 14px;
    color: #2d3748;
    vertical-align: top;
  }
  tbody td:first-child { font-weight: 600; color: #1a3c6e; }

  /* ── DRIVER BLOCKS ── */
  .driver-block {
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    margin-bottom: 20px;
    overflow: hidden;
  }
  .driver-header {
    background: linear-gradient(90deg, #f4a024 0%, #e8920f 100%);
    padding: 12px 20px;
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .driver-number {
    background: rgba(255,255,255,0.25);
    color: #fff;
    font-size: 12px;
    font-weight: 800;
    width: 26px; height: 26px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
  }
  .driver-header h3 {
    font-size: 14px;
    font-weight: 700;
    color: #fff;
  }
  .driver-body {
    padding: 18px 20px;
    background: #fffcf7;
  }
  .driver-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }
  .driver-item {
    background: #fff;
    border: 1px solid #f0e8d8;
    border-radius: 8px;
    padding: 12px 14px;
  }
  .driver-item-label {
    font-size: 11px;
    font-weight: 700;
    color: #f4a024;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    margin-bottom: 5px;
  }
  .driver-item-value {
    font-size: 13px;
    color: #2d3748;
    line-height: 1.55;
  }

  /* ── SCENARIO CARDS ── */
  .scenario-grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 18px;
    margin-top: 4px;
  }
  .scenario-card {
    border-radius: 12px;
    padding: 22px 20px;
    position: relative;
    overflow: hidden;
  }
  .scenario-bull {
    background: linear-gradient(145deg, #e8f5e9, #f1f8f1);
    border: 2px solid #4caf50;
  }
  .scenario-base {
    background: linear-gradient(145deg, #e3f0fb, #eef5fc);
    border: 2px solid #1a3c6e;
  }
  .scenario-bear {
    background: linear-gradient(145deg, #fdecea, #fdf3f2);
    border: 2px solid #e53935;
  }
  .scenario-label {
    font-size: 11px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 1.2px;
    margin-bottom: 10px;
  }
  .scenario-bull .scenario-label { color: #2e7d32; }
  .scenario-base .scenario-label { color: #1a3c6e; }
  .scenario-bear .scenario-label { color: #c62828; }
  .scenario-price {
    font-size: 26px;
    font-weight: 800;
    margin-bottom: 4px;
  }
  .scenario-bull .scenario-price { color: #2e7d32; }
  .scenario-base .scenario-price { color: #1a3c6e; }
  .scenario-bear .scenario-price { color: #c62828; }
  .scenario-upside {
    font-size: 13px;
    font-weight: 700;
    margin-bottom: 12px;
  }
  .scenario-prob {
    display: inline-block;
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 11px;
    font-weight: 700;
    margin-bottom: 12px;
  }
  .scenario-bull .scenario-prob { background: #c8e6c9; color: #1b5e20; }
  .scenario-base .scenario-prob { background: #bbdefb; color: #0d47a1; }
  .scenario-bear .scenario-prob { background: #ffcdd2; color: #b71c1c; }
  .scenario-assumptions {
    font-size: 12px;
    color: #4a5568;
    line-height: 1.6;
  }
  .scenario-assumptions li {
    margin-bottom: 4px;
    padding-left: 4px;
  }

  /* ── RISK SEVERITY BADGES ── */
  .badge {
    display: inline-block;
    padding: 3px 10px;
    border-radius: 12px;
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
  .badge-high { background: #ffcdd2; color: #c62828; }
  .badge-med  { background: #fff3cd; color: #856404; }
  .badge-low  { background: #d4edda; color: #155724; }

  /* ── CATALYST TIMEFRAME CHIPS ── */
  .tf-chip {
    display: inline-block;
    background: #e8f0fe;
    color: #1a3c6e;
    font-size: 11px;
    font-weight: 700;
    padding: 3px 10px;
    border-radius: 12px;
  }

  /* ── PROBABILITY BAR ── */
  .prob-bar-wrap { display: flex; align-items: center; gap: 8px; }
  .prob-bar {
    flex: 1;
    height: 8px;
    background: #e2e8f0;
    border-radius: 4px;
    overflow: hidden;
  }
  .prob-fill {
    height: 100%;
    border-radius: 4px;
    background: linear-gradient(90deg, #f4a024, #e8920f);
  }
  .prob-label { font-size: 12px; font-weight: 700; color: #1a3c6e; min-width: 36px; }

  /* ── CONCLUSION BOX ── */
  .conclusion-box {
    background: linear-gradient(135deg, #0d1b2a 0%, #1b3a5c 100%);
    border-radius: 14px;
    padding: 28px 32px;
    color: #fff;
    margin-top: 4px;
  }
  .conclusion-item {
    display: flex;
    gap: 16px;
    margin-bottom: 18px;
    align-items: flex-start;
  }
  .conclusion-icon {
    background: #f4a024;
    border-radius: 8px;
    width: 36px; height: 36px;
    display: flex; align-items: center; justify-content: center;
    font-size: 16px;
    flex-shrink: 0;
  }
  .conclusion-text h4 {
    font-size: 13px;
    font-weight: 700;
    color: #f4a024;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    margin-bottom: 5px;
  }
  .conclusion-text p {
    font-size: 13px;
    color: rgba(255,255,255,0.82);
    line-height: 1.65;
  }

  /* ── SUBSECTION LABEL ── */
  .subsec-label {
    font-size: 13px;
    font-weight: 700;
    color: #1a3c6e;
    border-bottom: 2px solid #f4a024;
    padding-bottom: 5px;
    margin: 22px 0 14px 0;
    display: inline-block;
  }

  /* ── TAILWIND / HEADWIND LIST ── */
  .th-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; margin-top: 6px; }
  .th-card {
    border-radius: 10px;
    padding: 16px 18px;
  }
  .th-tail { background: #e8f5e9; border-left: 4px solid #4caf50; }
  .th-head { background: #fdecea; border-left: 4px solid #e53935; }
  .th-card h4 { font-size: 12px; font-weight: 700; margin-bottom: 8px; text-transform: uppercase; letter-spacing: 0.6px; }
  .th-tail h4 { color: #2e7d32; }
  .th-head h4 { color: #c62828; }
  .th-card ul { padding-left: 16px; }
  .th-card li { font-size: